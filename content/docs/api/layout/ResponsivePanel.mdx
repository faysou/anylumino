---
title: "ResponsivePanel"
---

Box layout that switches direction when its width crosses a breakpoint.

## Attributes

<PyAttributes>
  <PyAttribute name="layout_kind" value="t.Unicode('responsive').tag(sync=True)" />

  <PyAttribute name="breakpoint" value="t.Int(760).tag(sync=True)" />

  <PyAttribute name="wide_direction" value="t.Enum(BOX_DIRECTIONS, default_value='left-to-right').tag(sync=True)" />

  <PyAttribute name="narrow_direction" value="t.Enum(BOX_DIRECTIONS, default_value='top-to-bottom').tag(sync=True)" />

  <PyAttribute name="spacing" value="t.Int(8).tag(sync=True)" />
</PyAttributes>

## Functions

<PyFunction name="__init__" type="(widgets=None, *, titles=None, keys=None, breakpoint=760, wide_direction='left-to-right', narrow_direction='top-to-bottom', spacing=8, width='100%', height=420, resizable=True, fit_content=False) -> None" kind="constructor">
  <PySourceCode>
    ```python
    def __init__(
        self,
        widgets: ChildInput = None,
        *,
        titles: TitleInput = None,
        keys: Iterable[str] | None = None,
        breakpoint: int = 760,
        wide_direction: str = "left-to-right",
        narrow_direction: str = "top-to-bottom",
        spacing: int = 8,
        width: int | float | str | None = "100%",
        height: int | float | str | None = 420,
        resizable: bool = True,
        fit_content: bool = False,
    ) -> None:
        self._init_composed(
            widgets,
            titles,
            keys,
            width,
            height,
            resizable,
            fit_content=fit_content,
            breakpoint=breakpoint,
            wide_direction=wide_direction,
            narrow_direction=narrow_direction,
            spacing=spacing,
        )
    ```
  </PySourceCode>

  <div>
    <PyParameter name="widgets" type="ChildInput" value="None">
      Child widgets or keyed child mapping.
    </PyParameter>

    <PyParameter name="titles" type="TitleInput" value="None">
      Optional labels associated with children.
    </PyParameter>

    <PyParameter name="keys" type="Iterable[str] | None" value="None">
      Stable child keys for iterable `widgets`.
    </PyParameter>

    <PyParameter name="breakpoint" type="int" value="760">
      Width in CSS pixels where the layout switches direction.
    </PyParameter>

    <PyParameter name="wide_direction" type="str" value="'left-to-right'">
      Box direction used when the container is wider than `breakpoint`.
    </PyParameter>

    <PyParameter name="narrow_direction" type="str" value="'top-to-bottom'">
      Box direction used when the container is narrower than `breakpoint`.
    </PyParameter>

    <PyParameter name="spacing" type="int" value="8">
      Pixel spacing between children.
    </PyParameter>

    <PyParameter name="width" type="int | float | str | None" value="'100%'">
      Widget width.
    </PyParameter>

    <PyParameter name="height" type="int | float | str | None" value="420">
      Widget height.
    </PyParameter>

    <PyParameter name="resizable" type="bool" value="True">
      Whether the panel gets a notebook resize handle.
    </PyParameter>

    <PyParameter name="fit_content" type="bool" value="False" />
  </div>

  <PyFunctionReturn type="None" />
</PyFunction>
