# Popover { #anylumino.Popover }

```python
Popover(children=None, *, open=True, placement='bottom', **kwargs)
```

Display child content in a popover surface.

## Parameters {.doc-section .doc-section-parameters}

| Name      | Type     | Description                                                                              | Default    |
|-----------|----------|------------------------------------------------------------------------------------------|------------|
| children  | optional | Child anywidgets or keyed child mapping rendered inside the component.                   | `None`     |
| open      | optional | Whether an overlay-like component starts open.                                           | `True`     |
| placement | optional | Preferred overlay placement such as ``'top'``, ``'bottom'``, ``'left'``, or ``'right'``. | `'bottom'` |