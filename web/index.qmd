---
title: "anylumino"
subtitle: "Lumino layouts for composable anywidgets"
---

`anylumino` is a small Python package for building notebook interfaces where
Lumino layout widgets arrange child anywidgets and ipywidgets controls.

::: {.hero-grid}
::: {.hero-copy}
The package gives notebooks an app-like layout vocabulary: tabs, split panes,
dock panels, scrollable control stacks, menus, command palettes, and keyed child
access from Python.
:::

![A stylized anylumino dashboard composed from panels, charts, tabs, and controls.](assets/anylumino-mark.svg){.hero-image}
:::

<div class="anylumino-demo" aria-label="Animated overview of anylumino layout composition">
<div class="demo-toolbar"><span></span><span></span><span></span><strong>Toolbar</strong></div>
<div class="demo-stage">
<div class="demo-tabs"><span class="active">Chart</span><span>Orders</span><span>Logs</span></div>
<div class="demo-split">
<div class="demo-controls"><i></i><i></i><i></i><i></i></div>
<div class="demo-chart"><svg viewBox="0 0 300 140" aria-hidden="true"><path class="line-a" d="M10 112 C60 62 91 98 132 72 S200 18 292 34"/><path class="line-b" d="M10 96 C68 80 95 38 145 58 S223 126 292 66"/></svg></div>
</div>
<div class="demo-dock"><span>Command palette</span><span>Dock panel</span></div>
</div>
</div>

The core idea is direct composition:

```python
from anylumino import SplitPanel, TabPanel, TextInput, IntSlider, VBox

controls = VBox(
    {
        "symbol": TextInput(value="AAPL", description="Symbol"),
        "rows": IntSlider(value=200, min=50, max=500, description="Rows"),
    },
    scroll=True,
    height=180,
)

tabs = TabPanel({"price": price_chart.widget, "volume": volume_chart.widget})
SplitPanel({"controls": controls, "charts": tabs}, sizes=[0.25, 0.75])
```

## What it provides

::: {.panel-tabset}

## Layouts

- `TabPanel` for tabbed child widgets.
- `BoxPanel`, `HBox`, `VBox`, and `ScrollBox` for linear composition.
- `SplitPanel` for drag-adjustable panes.
- `DockPanel`, `AccordionPanel`, `StackedPanel`, `GridPanel`, and
  `ResponsivePanel` for application-like notebook layouts.

## Actions

- `Toolbar` for compact command rows.
- `MenuBar` for grouped commands.
- `CommandPalette` for searchable actions.

Each action can dispatch to a Python callback.

## Controls

`anylumino` re-exports ipywidgets controls such as `TextInput`, `IntSlider`,
`Dropdown`, `Checkbox`, `Button`, `ListBox`, and `MultiSelect`. In Jupyter these
reuse the existing `@jupyter-widgets/controls` frontend.

:::

## Design model

`anylumino` is designed around `LayoutWidget`, the shared base class for layout
containers. A layout can be declared with a dictionary, and those keys become
the stable access path from Python:

```python
tabs = TabPanel({"price": price_chart, "orders": orders_table})
tabs.select_key("orders")
tabs["price"].fit()
tabs.call_owner("price", "fit")
```

That lets a dashboard own layout, state, and behavior without relying on global
variables.

## Next steps

- Read the [guide](guide.qmd) for the composition model.
- Review [controls](controls.qmd) for the ipywidgets re-exports.
- Open the [API reference](reference/index.qmd) generated by quartodoc.
